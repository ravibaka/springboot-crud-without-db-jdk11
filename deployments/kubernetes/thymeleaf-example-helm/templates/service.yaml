apiVersion: v1
kind: Service
metadata:
  name: {{ include "thymeleaf-example-helm.fullname" . }}
  labels:
    {{- include "thymeleaf-example-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "thymeleaf-example-helm.selectorLabels" . | nindent 4 }}
